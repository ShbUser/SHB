
<div class="container mt-3" id="invoice">


    <div class="row">
        <h1 class=""><u>Invoice</u><span><h6>SHB online shopping</h6></span></h1><br>        

        <div class="col-1 col-md-1 col-lg-1 col-xl-1 text-start">
            
            <br>
            Name:<br>
            Mobile:<br>
            pin:<br>
            City:<br>
            Landmark:<br>
        </div>
        <div class="col-3 col-md-3 col-lg-3 col-xl-3 text-start">
            
            <br>
            {{order.deliveryDetails.name}}<br>
            {{order.deliveryDetails.mobile}}<br>
            {{order.deliveryDetails.pin}}<br>
            {{order.deliveryDetails.city}}<br>
            {{order.deliveryDetails .landmark}}<br>
        </div>

        <div class="col-2 col-md-2 col-lg-2 col-xl-2 text-end">
            <br>
            Invoice no: <br>
            Date:<br>
            Order no: <br>
            Order date: <br>
            Payment: <br>
        </div>
        <div class="col-6 col-md-6 col-lg-6 col-xl-6 ">
            
            <br>
            108<br>
            01/09/2022<br>
            108<br>
            {{formatDate order.deliveryDetails.date}}<br>
            {{order.deliveryDetails.paymentMethod}}<br>
        </div>
    </div><br>
    <div class="row">
        <div class="col-7 col-md-7 col-lg-7 col-xl-7 ">

            <table class="table mt-3 " id="productsJS">
                <thead class="">
                    <tr>
                        <th scope="col">Product</th>
                        <th scope="col">Quantity</th>
                        <th scope="col">Price</th>

                    </tr>
                </thead>
                <tbody class="">
                    {{#each products}}
                    <tr>
                        <td>{{this.product.name}}</td>
                        <td>{{this.quantity}}</td>
                        <td>{{this.product.price}}</td>
                    </tr>
                    {{/each}}
                </tbody>
            </table>
        </div>
        <div class="col-5 col-md-5 col-lg-5 col-xl-5">

        </div>
    </div>
    <div class="row">
        <div class="col-7 col-md-7 col-lg-7 col-xl-7 text-end   ">
            <h6>Sub total : {{order.deliveryDetails.totalAmount}}</h6>
            <h6>Ship charge : ₹ 0.00</h6>
            <hr>
            <h4>Grand total : {{order.deliveryDetails.totalAmount}}</h4>
        </div>
    </div>

</div>
<div class="container">
<button class="btn p-0 btn-icon-text" onclick="printInvoice('invoice')"><i class="fa-solid fa-print"></i>
            Invoice</button>
            </div>

<script>
    function printInvoice(tagId) {
        var hashId = "#" + tagId;
        var tagname = $(hashId).prop("tagName").toLowerCase();
        var attributes = "";
        var attrs = document.getElementById(tagId).attributes;
        $.each(attrs, function (i, elem) {
            attributes += " " + elem.name + " ='" + elem.value + "' ";
        })
        var divToPrint = $(hashId).html();
        var head = "<html><head>" + $("head").html() + "</head>";
        var allContent = head + "<body  onload='window.print()' >" + "<" + tagname + attributes + ">" + divToPrint + "</" + tagname + ">" + "</body></html>";
        var newWin = window.open('', 'Print-Window');
        newWin.document.open();
        newWin.document.write(allContent);
        newWin.document.close();
        setTimeout(function () { newWin.close(); }, 1000);
    }
</script>