<div class="container">

    <section>
        <div class="row d-flex justify-content-center text-center bdrShdw">
            <div class="col-6 col-md-6 col-lg-6 col-xl-6">
                <form action="/admin/add-categories" method="post">
                    <input type="text" class="form-control" id="cat_name" name="name" placeholder="Category" required>
                    <input type="text" class="form-control" id="cat_name" name="description" placeholder="Description"
                        required>
                    <button type="submit" class="btn btn-outline-success"><i class="fa-solid fa-cloud-arrow-up"></i>Add Category</button>
                </form>
                    <br>
                
                <table class="table mt-3" id="categoryJS">
                    <thead class="bg-success ">
                        <tr>
                            <th scope="col">Category</th>
                            <th scope="col">Description</th>
                            <th>Options</th>
                        </tr>
                    </thead>
                    <tbody>
                        {{#each category}}
                        <tr>
                            <td>{{this.name}}</td>
                            <td>{{this.description}}</td>
                            <td>
                                <a href="/admin/edit_category/{{this._id}}" class="btn btn-outline-success">Edit <i
                                        class="fa-solid fa-file-pen"></i></a>
                                <a href="/admin/delete_category/{{this._id}}" class="btn btn-outline-danger"
                                    onclick="return confirm('Do u want to delete {{this.Name}} ?')">Delete<i
                                        class="fa-solid fa-trash"></i></a>
                            </td>
                        </tr>
                        {{/each}}
                    </tbody>
                </table>
            </div>
        </div>
</div>
</section>
<script>
    new simpleDatatables.DataTable("#categoryJS")
   
</script>